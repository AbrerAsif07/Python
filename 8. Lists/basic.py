# List is a sequence of unfixed length that can accomodate elements of any datatype

# while array is of fixed length that should be pre defined and data type should be
# defined and of that type only in that array/*

my_lst = [1, 2, 3, 4, 5, 6, 7]
# print(sum(my_lst))
# print(max(my_lst))
# print(len(my_lst))
n = len(my_lst)
print(my_lst[n - 2])
